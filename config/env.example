# Environment Variables for AQE

# JWT Secret for authentication tokens
JWT_SECRET=your-secret-key-here-change-in-production

# SMS Gateway Configuration
SMS_GATEWAY_URL=https://api.sms-gateway.com/send
SMS_GATEWAY_API_KEY=your-sms-api-key

# Application Environment
NODE_ENV=development

# Database Configuration (SQLite file will be created automatically)
DATABASE_PATH=./data/aqe.db

# Optional: External translation service API key
TRANSLATION_API_KEY=your-translation-api-key

# Optional: Push notification service
PUSH_SERVICE_KEY=your-push-service-key
